*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# /vendor is intentionally tracked (not ignored): .cpanel.yml deploys this
# directory with a plain `cp -R`, no `composer install` step, and no shell
# access is assumed on the host - so vendor/ has to already be here, the same
# way frontend/dist is committed instead of built on the server.
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
